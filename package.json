{
  "name": "cryptarch-quiz",
  "version": "1.0.0",
  "main": "",
  "scripts": {
    "start": "npm start --prefix server",
    "install-frontend": "cd frontend && npm install && npm run build && cd ..",
    "install-server": "cd server && npm install && npm run build && cd .. ",
    "heroku-postbuild": "npm run install-frontend && npm run install-server"
  }
}
