{
  "name": "cryptarch-quiz",
  "version": "1.0.0",
  "main": "",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "start": "npm start --prefix server",
    "install-server": "cd server && npm install && npm run build && cd .. ",
    "heroku-postbuild": "npm run install-server && npm run start"
  }
}
